#include "point.h"

void Point2D::Translate(double dx, double dy)
{
    this->setX(this->getX() + dx);
    this->setY(this->getY() + dy);
}
void Point2D::RotateZ(double angle)
{
    //Calculo super complexo ira aparecer aqui!!!!!
}
